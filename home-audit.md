# home-audit

`~/vault` の外で更新されたファイルを可視化するための監査スクリプト。

## 目的

このリポジトリ全体の方針は「バックアップ対象は `~/vault` のみ、それ以外は消えて良い」である。
しかし日常の業務では、意図しない場所にファイルが書き込まれる。
気をつけるだけでは徹底できているかどうか確認できない。

home-audit は「気をつける」を「検出する」に置き換えるためのツールである。
vaultの外で最近更新されたファイルを列挙し、
そのひとつひとつについて判断を下すことを促す。

**このスクリプトは何も削除しない。** 検出と報告のみを行う。

## 使い方

```sh
home-audit          # 直近 7 日
home-audit 30       # 直近 30 日
home-audit 7 -v     # ファイルパスを個別に表示
```

### 引数

| 位置 | 内容 | 既定値 |
|---|---|---|
| 1 | 対象期間（日数） | `7` |
| 2 | `-v` を渡すと詳細表示 | なし |

### 終了コード

| 値 | 意味 |
|---|---|
| 0 | 常に 0（検出の有無にかかわらず） |

検出件数で終了コードを変えていない。
CI ではなく人間が読むためのツールであり、
「0 件でないと失敗」という運用は現実的でないため。

## 出力

### 集約モード（既定）

vault外で更新されたファイルを、ホームからの相対パスで2階層目まで丸めて集計する。

```
3197 file(s) modified outside the vault in the last 30 days:

2553 Pictures/Photos Library.photoslibrary
 430 .claude/plugins
  93 Music/Music
  54 .config/raycast
  30 .zsh_sessions
   4 (home)
```

`(home)` はホーム直下に置かれたファイルを指す。

### 詳細モード（`-v`）

検出したファイルを1行ずつ、`~/` 始まりのパスで列挙する。
集約結果の内訳を確認したいときに使う。

### 検出ゼロの場合

```
clean: no files modified outside the vault in the last 30 days.
```

**この表示が出たときは疑うこと。** 後述の「既知の失敗モード」を参照。

## 除外の仕組み

2種類の除外がある。どちらも `find` の `-prune` として展開される。

### `EXCLUDES` — 絶対パスでの除外

特定のディレクトリツリー全体を対象外にする。

```bash
EXCLUDES=(
  "$HOME/vault"                  # vaultそのもの
  "$HOME/src"                    # ghq 管理下。ghq get で再取得できる
  "$HOME/Library"                # アプリ設定。再現可能側に倒す判断
  "$HOME/.Trash"
  "$HOME/.cache"                 # XDG_CACHE_HOME
  "$HOME/.local/share/mise"      # mise が管理するツール本体
  "$HOME/.local/state"           # XDG_STATE_HOME
  "$HOME/.npm"
  "$HOME/.bun"
  "$HOME/.docker"
)
```

### `PRUNE_NAMES` — ディレクトリ名での除外

どこにあっても対象外にする名前。

```bash
PRUNE_NAMES=(
  node_modules
  .git
  .venv
  target
  dist
  build
  .next
)
```

### 除外リストは「消して良いものの仕様書」である

エントリを追加するときは、**なぜ除外して良いかをコメントに残すこと。**
理由の書かれていない除外は、後から見て正しさを検証できない。

除外の判断基準は3分類に従う。

| 分類 | 判定 | 例 |
|---|---|---|
| 再現可能 | 除外 | mise が入れるツール、ghq 配下のリポジトリ |
| 使い捨て | 除外 | キャッシュ、ビルド成果物、セッション履歴 |
| 再現不能 | **除外しない。`~/vault` に移す** | 自分が作ったもの、もらったもの |

`~/Library` を除外しているのは「アプリ設定は捨てる」という明示的な判断による。
Photos ライブラリ、Mail のローカルアーカイブ、ライセンス情報など、
`~/Library` 配下にも再現不能なものは存在しうる。
この判断は初期化リハーサルのたびに再検証すること。

## 運用

### 週次

`home-audit` を実行し、出力された各エントリについて、以下のいずれかを確定させる。

- **vaultに移す** — 再現不能なもの。`~/vault` 配下へ移動する
- **除外に追加する** — 再現可能か使い捨て。`EXCLUDES` に理由付きで追記する

一度に全部を処理する必要はない。上位から順に潰していけばよい。

### 完了の定義

出力が `clean:` に近づき、
新しく現れるエントリが「まだ判定していないもの」だけになった状態。

このとき初めて「vaultの外は消して良い」と言い切れる。

### 初期化リハーサルとの関係

home-audit と初期化リハーサルは、それぞれ違う穴を塞ぐ。

| 手段 | 検出できるもの |
|---|---|
| home-audit | 知らないうちにvault外に書き込まれたもの |
| 初期化リハーサル | 復元に必要なのにvaultに入れ忘れたもの |

両方やって初めて穴が塞がる。
home-audit を数週間回して安定させてから、リハーサルに進むとよい。

## 実装上の注意

### bash のバージョン

macOS 標準の bash は 3.2 である。
`mapfile` / `readarray` は bash 4.0 以降の機能なので使用していない。

配列は素朴なループで組み立てている。

```bash
FIND_ARGS=()
for p in "${EXCLUDES[@]}"; do
  FIND_ARGS+=(-path "$p" -prune -o)
done
```

### 既知の失敗モード

**検出ゼロなのに実際は書き込みがある**

`find` に渡す引数の組み立てに失敗すると、
検索対象が空になり `clean:` と表示される。
監査ツールが黙って壊れ、安心だけが残る最悪のパターンである。

過去に `mapfile` が bash 3.2 に存在しないことが原因で発生した。
エラーは表示されたが、その後に `clean:` が出力されたため見落としやすかった。

**初回実行で `clean:` が出たら、まず疑うこと。**
ホーム直下には `.zsh_history` などが必ず存在するため、
何も検出されないのは不自然である。

動作確認には、既知のファイルが検出されるかを見るとよい。

```sh
touch ~/audit-canary && home-audit 1 -v | grep audit-canary && rm ~/audit-canary
```

### 配置

`~/vault/dotfiles/.local/bin/home-audit` に置き、
`[dotfiles]` の `copy` モードで `~/.local/bin/` へ配置する。

```toml
"~/.local/bin/home-audit" = { mode = "copy", mode_bits = "0755" }
```

symlink ではなく copy を使っているのは実行権限のため。
**copy モードはソースを編集しただけでは反映されない。**
`mise bootstrap --only dotfiles` を実行すること。

## 将来の検討事項

### 自動実行

`[bootstrap.macos.launchd.agents]` で週次実行を宣言できる。
ただし除外リストが安定するまでは手動で回すこと。
毎回大量の出力が届くと、通知そのものを見なくなる。

### リアルタイム調査

特定のアプリがどこに書き込むかを調べたいときは `fs_usage` が使える。

```sh
sudo fs_usage -w -f filesys | grep <app-name>
```

新しいツールを導入したときのスポット調査に向く。
home-audit は事後の検出、`fs_usage` は事前の調査という使い分け。
